VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# If these are unset, app will use the localStorage mock backend.

# Server-side (do NOT commit service role keys)
# Required for the serverless grantPoints endpoint using the Supabase service role key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# Admin secret used to authorize calls to the serverless endpoint
ADMIN_SECRET=super-secret-admin-key
